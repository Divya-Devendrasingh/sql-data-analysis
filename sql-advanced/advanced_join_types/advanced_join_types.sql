-- LEFT JOIN
SELECT *
FROM A
LEFT JOIN B
ON A.id = B.id;

-- RIGHT JOIN
SELECT *
FROM A
RIGHT JOIN B
ON A.id = B.id;

-- FULL OUTER JOIN
SELECT *
FROM A
FULL OUTER JOIN B
ON A.id = B.id;

-- CROSS JOIN
SELECT *
FROM A
CROSS JOIN B;
